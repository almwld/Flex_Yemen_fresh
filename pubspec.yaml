name: flex_yemen_fresh
description: Flex Yemen Modern App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"  # تحديث إصدار SDK

dependencies:
  flutter:
    sdk: flutter
  
  # الأساسيات فقط - بدون إضافات قد تسبب مشاكل
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10+1
  cached_network_image: ^3.4.0
  provider: ^6.1.2
  shimmer: ^3.0.0
  intl: ^0.19.0
  
  # Supabase - الإصدارات المتوافقة
  supabase_flutter: ^2.5.3
  supabase: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
